import { TwoDecimalPipePipe } from './two-decimal-pipe.pipe';

describe('TwoDecimalPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TwoDecimalPipePipe();
    expect(pipe).toBeTruthy();
  });
});
